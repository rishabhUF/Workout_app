#workout
	%p= @workout.date
	%p= @workout.workout
	%p= @workout.mood
	%p= @workout.length

	#exercises
		%h3 Add Exercises
		=render @workout.exercises
		=render 'exercises/exercises_form'
= link_to "Back", root_path
= link_to "Edit", edit_workout_path
= link_to "Delete", @workout, method: :delete,data: {confirm: "Are you sure"}