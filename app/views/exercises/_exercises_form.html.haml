= simple_form_for([@workout, @workout.exercises.build], html: {class: 'form-horizontal'}) do |f|
	= f.input :name, label:"Name"
	= f.input :sets, label:"How many sets"
	= f.input :reps, label:"How are many reps"
	%br/
	= f.button :submit